pipeline {
  agent any 
  stages {
    stage ('build code'){
      steps {
        echo "building artifact"
      }
    }
    stage ('deploy code') {
      steps {
        echo "deploying code"
      }
    }
  }
}
